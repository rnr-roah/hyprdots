-- Your old general.conf used $primary and $on_primary.
-- Replace these with your real matugen values if you generate colors elsewhere.
-- If you already have a generated Lua color module, make it return the same fields.

local colors = {
  -- fallback from your commented old config
  primary = "rgba(968f8ddd)",
  on_primary = "rgba(000000ee)",
}

return colors
